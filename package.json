{
  "name": "task-ticket-backend",
  "version": "1.0.0",
  "description": "backend for tickets to tasks which will be used in enotes system development",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV = production node server",
    "dev": "nodemon server"
  },
  "engines": {
    "node": "10.16.0"
  },
  "author": "Swastik Thapaliya",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.9.22",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.4"
  }
}
